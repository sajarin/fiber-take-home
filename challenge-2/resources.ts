/**
 * Read this CSV file to
 */
export const CSV_INPUT_PATH: string = "inputs/companies.csv";
export const TEST_CSV_INPUT_PATH: string = "inputs/test.csv";

/**
 * Your JSON file, containing scraped and structured data from all the companies in
 * `companies.csv`, should live here.
 */
export const JSON_OUTPUT_PATH: string = "out/scraped.json";
